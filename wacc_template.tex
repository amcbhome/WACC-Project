\documentclass[a4paper,12pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb}
\usepackage{booktabs}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=black, citecolor=black, urlcolor=blue}

\usepackage[style=authoryear,backend=biber]{biblatex}
\addbibresource{references.bib}

\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}

\section*{Executive Summary}
This report computes the Weighted Average Cost of Capital (WACC) for
\textbf{{ COMPANY_NAME }} following methods in \textcite{WatsonHead2019}.

Key Results:
\begin{itemize}
  \item WACC (Book): \textbf{{ WACC_BV_PCT }}\%
  \item WACC (Market): \textbf{{ WACC_MV_PCT }}\%
\end{itemize}

\section{Step One: Cost of Finance Sources}
\subsection*{Cost of Equity}
\textbf{Method:} {{ EQUITY_METHOD }}

{% if EQUITY_METHOD == 'CAPM' -%}
\[
R_e = R_f + \beta (MRP)
\]
Inputs used: Company = \textit{{ CAPM_COMPANY }}, \quad \(R_f = {{ RF_PCT }}\), \quad \(MRP = {{ MRP_PCT }}\), \quad \(\beta = {{ BETA_VAL }}\). \\
Result: \textbf{{ EQUITY_COST_PCT }}
{% else -%}
\[
R_e = \frac{D_1}{P_0} + g \quad \text{with} \quad D_1 = D_0(1+g)
\]
Inputs used: \(D_0 = {{ D0_VAL }}\), \quad \(g = {{ G_PCT }}\), \quad \(P_0 = {{ P0_EQUITY }}\). \\
Result: \textbf{{ EQUITY_COST_PCT }}
{% endif %}

\subsection*{Other Sources (after tax where applicable)}
\begin{itemize}
  \item Preference shares: \textbf{{ PREF_COST_PCT }}
  \item Redeemable debt: \textbf{{ RED_COST_PCT }}
  \item Irredeemable debt: \textbf{{ IRRED_COST_PCT }}
  \item Bank loans: \textbf{{ BANK_COST_PCT }}
\end{itemize}

\section{Step Two: Capital Structure Weights}
\begin{center}
\begin{tabular}{lcc}
\toprule
\textbf{Source} & \textbf{Weight (BV)} & \textbf{Weight (MV)} \\
\midrule
{% for r in ROWS_BV -%}
{{ r.source }} & {{ "%.4f"|format(r.weight) }} & {{ "%.4f"|format(ROWS_MV[loop.index0].weight) }} \\
{% endfor %}
\midrule
\textbf{Total} & 1.0000 & 1.0000 \\
\bottomrule
\end{tabular}
\end{center}

\section{Step Three: WACC Calculation}
\begin{center}
\begin{tabular}{lccc}
\toprule
\textbf{Source} & \textbf{Cost (\%)} & \textbf{Weight (BV)} & \textbf{Weight (MV)} \\
\midrule
{% for r in ROWS_BV -%}
{{ r.source }} & {{ "%.2f"|format(r.cost*100) }} & {{ "%.4f"|format(r.weight) }} & {{ "%.4f"|format(ROWS_MV[loop.index0].weight) }} \\
{% endfor %}
\midrule
\textbf{WACC (Book)} & \multicolumn{3}{r}{\textbf{{ WACC_BV_PCT }}\%} \\
\textbf{WACC (Market)} & \multicolumn{3}{r}{\textbf{{ WACC_MV_PCT }}\%} \\
\bottomrule
\end{tabular}
\end{center}

{{ FORENSIC_BLOCK }}

\printbibliography
\end{document}
